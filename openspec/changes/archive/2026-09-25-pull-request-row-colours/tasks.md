# Tasks

## 1. Forge

- [x] 1.1 Ask `gh pr view` for `mergeStateStatus`
- [x] 1.2 Split `Mergeability` into `Conflicting`, `Behind`, `ChecksRunning`, `Blocked`, `Mergeable`, `Unknown` with the precedence in `design.md`
- [x] 1.3 Tests for each reason and each precedence edge

## 2. View

- [x] 2.1 `state_color`: green, orange, yellow, blue for open rows
- [x] 2.2 `detail_line`: no checks for merged or closed rows
- [x] 2.3 Tests for both
